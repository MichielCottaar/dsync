Syncing data across multiple machines (ssh) and backups

# Installation
```shell
pip install git+https://github.com/MichielCottaar/dsync.git
```

## Releasing new versions
- Run `bump2version` (install using `pip install bump2version`)
- Push to gitlab
